service:
  name: polyglot-backend
  host: 0.0.0.0
  port: 8080

database:
  engine: postgres
  host: localhost
  port: 5432
  user: app_user
  password: changeme
  name: polyglot
  pool_size: 5

redis:
  host: localhost
  port: 6379
  db: 0

api:
  base_url: http://localhost:8080
  timeout_seconds: 30

features:
  enable_payments: true
  enable_experiments: false
  legacy_mode: false
